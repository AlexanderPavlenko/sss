```
Usage: sss [options]
        --reconstruct                Reconstruct secret while reading shares from STDIN
        --split AVAILABLE,NEEDED     Split secret into AVAILABLE number of shares, NEEDED of which are required to reconstruct secret
        --random N                   Generate random secret of N bytes instead of reading secret from STDIN
        --quiet                      Use STDOUT only for data output
```
